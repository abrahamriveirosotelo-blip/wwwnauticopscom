# Build / deps
dist
build
coverage
node_modules

# Lockfiles
bun.lockb
package-lock.json

# Datos generados por el cron / scrapers — no formatear (evita churn y conflictos con las escrituras automáticas)
src/pages/demos/**/data.json
src/pages/demos/**/vessel-cache.json

# Fixtures de scrapers — HTML/PDF crudo capturado de las fuentes (no es código; Prettier no lo parsea)
scripts/fixtures

# Demo v2 autocontenida (#75): un único HTML con JS/CSS inline que Vercel sirve tal cual.
# Formatearlo reescribiría ~5.300 líneas y no aporta nada: no entra en el build de Vite.
# El resto de public/ (favicon, robots.txt, sitemap.xml, _redirects, .svg) no necesita entrada:
# Prettier no infiere parser para esas extensiones y ya las ignora.
public/demo/v2

# Ajustes locales de agentes: existen solo en la máquina de cada uno y están en el
# gitignore GLOBAL, que Prettier no lee — sin esta línea, format:check sale rojo por
# un fichero que ni siquiera está en el repo.
.claude/settings.local.json

# Minificados
*.min.*
